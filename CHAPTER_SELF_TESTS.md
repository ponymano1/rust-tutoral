# Rust 章节自测题

使用方式：每学完一个 `src/bin/NN_*.rs` 章节后，先不看教程源码，自己新建或修改一个小程序完成本章自测。能独立写出代码并解释运行结果，就算通过。

## 01 Hello World：程序入口与输出

- 写一个 `main` 函数。
- 使用 `println!` 输出一句自定义文本。
- 修改输出内容并重新运行，确认程序入口只从 `main` 开始执行。

## 02 Variables：变量与可变性

- 声明一个不可变变量并打印它。
- 声明一个 `mut` 变量，先赋一个值，再修改它并打印。
- 尝试修改不可变变量，观察编译器报错并说明原因。

## 03 Types：基础类型

- 声明一个 4 字节有符号整数变量。
- 声明一个 8 字节浮点数变量。
- 声明一个布尔变量和一个字符变量。
- 给至少两个变量显式标注类型，例如 `i32`、`f64`、`bool`、`char`。

## 04 Shadowing：变量遮蔽

- 使用同一个变量名连续声明两次，让第二个值遮蔽第一个值。
- 用遮蔽改变变量的值。
- 用遮蔽改变变量的类型，例如从数字变成字符串切片。
- 解释遮蔽和 `mut` 的区别。

## 05 Const：常量

- 声明一个 `const` 常量并打印。
- 给常量显式标注类型。
- 尝试不给常量标注类型，观察编译器提示。
- 说明常量和普通不可变变量的区别。

## 06 Function：函数

- 写一个带参数的函数，接收两个整数并打印它们。
- 写一个返回整数的函数。
- 使用表达式作为函数返回值，不写 `return`。
- 在 `main` 中调用这两个函数。

## 07 If：条件表达式

- 使用 `if / else` 判断一个数字是否大于某个值。
- 把 `if / else` 的结果赋值给一个变量。
- 确保 `if` 和 `else` 两个分支返回相同类型。
- 尝试让两个分支返回不同类型，观察编译器报错。

## 08 Loop：循环

- 使用 `for` 循环遍历 `0..3`。
- 修改范围，让循环打印 1 到 5。
- 使用循环对一组数字求和。
- 解释 `0..3` 是否包含 3。

## 09 Scope：作用域

- 在内部代码块中声明一个变量并打印。
- 尝试在代码块外访问这个变量，观察编译器报错。
- 在内外两个作用域中使用同名变量，观察打印结果。
- 说明变量什么时候离开作用域。

## 10 Ownership：所有权

- 创建一个 `String`，把它赋值给另一个变量。
- 尝试继续使用原变量，观察编译器报错。
- 把一个 `String` 传给函数，观察所有权是否被移动。
- 用自己的话解释“同一时间只有一个所有者”。

## 11 Reference：不可变引用

- 写一个函数，接收 `&String` 参数并打印内容。
- 在 `main` 中创建 `String`，把引用传给函数。
- 函数调用后继续使用原来的 `String`。
- 说明引用为什么不会拿走所有权。

## 12 Mutable Reference：可变引用

- 写一个函数，接收 `&mut String` 并追加内容。
- 在 `main` 中创建 `mut String`，把可变引用传给函数。
- 函数调用后打印修改后的字符串。
- 尝试同时创建两个可变引用，观察编译器报错。

## 13 Slice：切片

- 创建一个 `String`，取出其中一部分 `&str` 切片。
- 打印完整字符串和切片。
- 写一个函数，接收 `&str` 参数。
- 分别把字符串字面量和 `String` 的切片传给这个函数。

## 14 Struct：结构体

- 定义一个包含两个字段的结构体。
- 创建一个结构体实例。
- 读取并打印结构体字段。
- 创建第二个实例，给字段赋不同的值。

## 15 Impl：方法

- 给一个结构体写 `impl` 块。
- 实现一个使用 `&self` 的方法。
- 在方法中读取结构体字段并返回或打印结果。
- 在 `main` 中通过实例调用这个方法。

## 16 Enum：枚举

- 定义一个包含多个变体的枚举。
- 创建不同枚举变体的值。
- 使用 `match` 匹配枚举变体。
- 给至少一个枚举变体携带数据，并在匹配时取出它。

## 17 Match：模式匹配

- 使用 `match` 匹配一个整数。
- 至少写三个明确分支。
- 添加 `_` 通配分支处理其他情况。
- 说明为什么 `match` 必须覆盖所有可能情况。

## 18 Option：可选值

- 创建一个 `Some` 值和一个 `None` 值。
- 写一个函数，返回 `Option<i32>`。
- 使用 `match` 处理 `Some` 和 `None`。
- 解释 `Option` 如何避免空值问题。

## 19 Result：成功与失败

- 创建一个 `Ok` 值和一个 `Err` 值。
- 使用 `match` 分别处理成功和失败。
- 写一个可能失败的函数，返回 `Result<T, E>`。
- 说明 `Result` 和 `Option` 的区别。

## 20 Vec：动态数组

- 创建一个空 `Vec<i32>`。
- 使用 `push` 添加至少三个元素。
- 使用 `for` 循环遍历并打印每个元素。
- 尝试读取某个索引位置的元素。

## 21 String：字符串

- 创建一个 `String`。
- 使用 `push_str` 拼接字符串。
- 使用 `format!` 生成一个新字符串。
- 说明 `String` 和 `&str` 的区别。

## 22 HashMap：键值对集合

- 创建一个 `HashMap`。
- 插入至少两个键值对。
- 根据 key 读取一个值。
- 使用 `for` 循环遍历所有键值对。

## 23 Module：模块

- 定义一个模块。
- 在模块中写一个函数。
- 使用 `pub` 暴露这个函数。
- 在模块外调用这个函数。

## 24 Use：导入路径

- 使用完整路径调用一个标准库类型或函数。
- 使用 `use` 简化路径。
- 对比简化前后的调用方式。
- 尝试导入模块中的公开函数并调用它。

## 25 Error Handling：错误传播

- 写一个返回 `Result` 的函数。
- 在函数中调用另一个返回 `Result` 的操作。
- 使用 `?` 传播错误。
- 在 `main` 或调用方中处理最终结果。

## 26 File IO：文件读写

- 使用标准库写入一个文本文件。
- 从这个文件读取内容。
- 打印读取到的内容。
- 把文件操作错误用 `Result` 返回或处理。

## 27 Generic：泛型

- 写一个泛型函数，接收某种类型的参数。
- 给泛型添加 trait bound。
- 用不同类型调用这个泛型函数。
- 解释为什么泛型函数有时需要 trait bound。

## 28 Trait：特征

- 定义一个 trait，包含一个方法签名。
- 定义一个结构体并为它实现这个 trait。
- 调用 trait 方法。
- 再定义第二个结构体，实现同一个 trait。

## 29 Lifetime：生命周期

- 写一个函数，接收两个字符串切片引用。
- 使用显式生命周期参数标注输入和返回值。
- 返回两个字符串切片中较长的那个。
- 解释返回引用为什么需要和输入引用建立生命周期关系。

## 30 Closure：闭包

- 定义一个闭包，接收一个数字并返回它的两倍。
- 调用闭包并打印结果。
- 定义一个捕获外部变量的闭包。
- 对比闭包和普通函数的写法差异。

## 31 Iterator：迭代器

- 创建一个数字数组或 `Vec`。
- 使用 `.iter()` 获取迭代器。
- 使用 `.map()` 转换每个元素。
- 使用 `.sum()` 求和并打印结果。

## 32 Test：单元测试

- 写一个普通函数，例如两个数字相加。
- 在 `#[cfg(test)]` 模块中写测试。
- 使用 `#[test]` 标记测试函数。
- 使用 `assert_eq!` 验证函数结果。

## 综合自测

完成所有章节后，尝试写一个小程序，要求：

- 定义一个结构体表示用户或账户。
- 使用 `impl` 给它添加方法。
- 使用 `Vec` 或 `HashMap` 保存多个实例。
- 使用 `match`、`Option` 或 `Result` 处理查找结果。
- 至少写一个单元测试验证核心逻辑。
