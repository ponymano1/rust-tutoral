# 重要问题
参照rust 官方的 通过例子学rust

## 9.2.2 闭包作为输入参数

```rust
fn apply<F>(f: F) where
    // 闭包没有输入值和返回值。
    F: FnOnce() {
    // ^ 试一试：将 `FnOnce` 换成 `Fn` 或 `FnMut`。
    f();
}
```
这个where等价于
```rust
  fn apply<F: FnOnce()>(f: F) {                                                                               
      f();                                                         
  }   
```
  是在说：F 必须实现 FnOnce()                                                                                          
                      

## 14.3 泛型 trait

```rust
// 不可复制的类型。
struct Empty;
struct Null;

// `T` 的泛型 trait。
trait DoubleDrop<T> {
    // 定义一个调用者的方法，接受一个额外的参数 `T`，但不对它做任何事。
    fn double_drop(self, _: T);
}

// 对泛型的调用者类型 `U` 和任何泛型类型 `T` 实现 `DoubleDrop<T>` 。
impl<T, U> DoubleDrop<T> for U {
    // 此方法获得两个传入参数的所有权，并释放它们。
    fn double_drop(self, _: T) {}
}

fn main() {
    let empty = Empty;
    let null  = Null;

    // 释放 `empty` 和 `null`。
    empty.double_drop(null);

    //empty;
    //null;
    // ^ 试一试：去掉这两行的注释。
}
```

## 14.6 泛型， where 分句
```rust
impl <A: TraitB + TraitC, D: TraitE + TraitF> MyTrait<A, D> for YourType {}

// 使用 `where` 从句来表达约束
impl <A, D> MyTrait<A, D> for YourType where
    A: TraitB + TraitC,
    D: TraitE + TraitF {}

```

## 15.3.2 可变引用与不可变引用同时存在规则
数据可以多次不可变借用，但是在不可变借用的同时，原始数据不能使用可变借用。或者说，同一时间内只允许一次可变借用。仅当最后一次使用可变引用之后，原始数据才可以再次借用。

### 不可变借用还存在，并且之后有使用的话，不能声明不可变借用
```rust
    let mut point = Point { x: 0, y: 0, z: 0 };

    let borrowed_point = &point;
    let another_borrow = &point;
    
    // 报错！`point` 不能以可变方式借用，因为当前还有不可变借用。后面还使用了不可变借用。
    // 如果后面不适用，那么这里不会报错
    let mutable_borrow = &mut point;
    // 被借用的值在这里被重新使用, 正式由于在这里使用了不可变引用，所以前面的可变引用会报错
    println!("Point has coordinates: ({}, {}, {})",
                borrowed_point.x, another_borrow.y, point.z);
```

### 可变引用还在，并且后面有被用到，那么不能使用不可变方式来借用
```rust
    // 不可变的引用不再用于其余的代码，因此可以使用可变的引用重新借用。
    let mutable_borrow = &mut point;

    // 通过可变引用来修改数据
    mutable_borrow.x = 5;
    mutable_borrow.y = 2;
    mutable_borrow.z = 1;

    // 报错！不能再以不可变方式来借用 `point`，因为它当前已经被可变借用。
    // let y = &point.y;

    // 报错！无法打印，因为 `println!` 用到了一个不可变引用。
    // println!("Point Z coordinate is {}", point.z);

    // 正常运行！可变引用能够以不可变类型传入 `println!`
    println!("Point has coordinates: ({}, {}, {})",
                mutable_borrow.x, mutable_borrow.y, mutable_borrow.z);

    // 可变引用不再用于其余的代码，因此可以重新借用
    let new_borrowed_point = &point;
    println!("Point now has coordinates: ({}, {}, {})",
             new_borrowed_point.x, new_borrowed_point.y, new_borrowed_point.z);
```

## 关键问题：什么时候算借用结束
    - 传统的代码块返回后
    - 引用在最后一次使用后立即结束(rust 2018后)


## 16.2 使用 dyn 返回 trait
```rust
// 返回一些实现 Animal 的结构体，但是在编译时我们不知道哪个结构体。
fn random_animal(random_number: f64) -> Box<dyn Animal> {
    if random_number < 0.5 {
        Box::new(Sheep {})
    } else {
        Box::new(Cow {})
    }
}
```

## 16.5 迭代器
迭代器 take 和 skip
```rust
    // `take(n)` 方法提取 `Iterator` 的前 `n` 项。
    println!("The first four terms of the Fibonacci sequence are: ");
    for i in fibonacci().take(4) {
        println!("> {}", i);
    }

    // `skip(n)` 方法移除前 `n` 项，从而缩短了 `Iterator` 。
    println!("The next four terms of the Fibonacci sequence are: ");
    for i in fibonacci().skip(4).take(4) {
        println!("> {}", i);
    }
```

### trait 默认类型参数
- 扩展trait而不破坏现有代码
- 允许特定情况下自定义
- 模版里`Rhs=Self` 就是表示默认类型为Self
```rust
trait Add<Rhs=Self> {
    type Output;
    fn add(self, rhs: Rhs) -> Self::Output;
}
```

## 16.9 消除重叠trait
- 使用as `<Form as UsernameWidget>::get(&form);`
```rust
trait UsernameWidget {
    // 从这个 widget 中获取选定的用户名
    fn get(&self) -> String;
}

trait AgeWidget {
    // 从这个 widget 中获取选定的年龄
    fn get(&self) -> u8;
}

// 同时具有 UsernameWidget 和 AgeWidget 的表单
struct Form {
    username: String,
    age: u8,
}

impl UsernameWidget for Form {
    fn get(&self) -> String {
        self.username.clone()
    }
}

impl AgeWidget for Form {
    fn get(&self) -> u8 {
        self.age
    }
}

fn main() {
    let form = Form{
        username: "rustacean".to_owned(),
        age: 28,
    };

    // 如果取消注释此行，则会收到一条错误消息，提示 “multiple `get` found”（找到了多个`get`）。
    // 因为毕竟有多个名为 `get` 的方法。
    // println!("{}", form.get());

    let username = <Form as UsernameWidget>::get(&form);
    assert_eq!("rustacean".to_owned(), username);
    let age = <Form as AgeWidget>::get(&form);
    assert_eq!(28, age);
}

```

## 18.2.2 错误处理，Option 组合算子map
- Some->Some, None->None
 ```rust
  let some_value: Option<i32> = Some(42);
  let mapped_value = some_value.map(|x| x * 2);
  
  assert_eq!(mapped_value, Some(84));
  
  let none_value: Option<i32> = None;
  let mapped_value = none_value.map(|x| x * 2);
  
  assert_eq!(mapped_value, None);
  ```
## Option 其他简化操作
- and_then
- unwrap, expect, uwrap_or, unwrap_or_else

 ```rust
  let some_value: Option<i32> = Some(42);
  let result = some_value.and_then(|x| Some(x * 2));

  let some_value: Option<i32> = Some(42);
  let value = some_value.unwrap();

  let some_value: Option<i32> = Some(42);
  let value = some_value.unwrap_or(-1);

  let value = none_value.unwrap_or_else(|| generate_default_value());
```

## 18.3.1
- Result map和and_then
```rust
fn multiply(first_number_str: &str, second_number_str: &str) -> Result<i32, ParseIntError> {
    first_number_str.parse::<i32>().and_then(|first_number| {
        second_number_str.parse::<i32>().map(|second_number| first_number * second_number)
    })
}
```

## 18.4.1 Option和result 组合和转换

```rust
fn double_first(vec: Vec<&str>) -> Option<Result<i32, ParseIntError>> {
    vec.first().map(|first| {
        first.parse::<i32>().map(|n| 2 * n)
    })
}
```

### 如果option返回None要继续处理错误，则可以使用一些组合算子来轻松实现
```rust
fn double_first(vec: Vec<&str>) -> Result<Option<i32>, ParseIntError> {
    let opt = vec.first().map(|first| {
        first.parse::<i32>().map(|n| 2 * n)
    });

    opt.map_or(Ok(None), |r| r.map(Some))
}
```

## 18.4 处理多种错误类型(未看)

## 18.5 遍历result

### 使用filter_map来忽略失败项
- filter_map会调用一个函数，过滤到为None的所有结果
```rust
fn main() {
    let strings = vec!["tofu", "93", "18"];
    let numbers: Vec<_> = strings
        .into_iter()
        .filter_map(|s| s.parse::<i32>().ok())
        .collect();
    println!("Results: {:?}", numbers);
}
```

### 使用Partition() 收集所有合法的值与错误
- 在组合操作中如果有Err, collect操作就被终止
- 使用Partition代替collect

```rust
fn main() {
    let strings = vec!["tofu", "93", "18"];
    let (numbers, errors): (Vec<_>, Vec<_>) = strings
        .into_iter()
        .map(|s| s.parse::<i32>())
        .partition(Result::is_ok);
    println!("Numbers: {:?}", numbers);
    println!("Errors: {:?}", errors);
}
//结果
// Numbers: [Ok(93), Ok(18)]
// Errors: [Err(ParseIntError { kind: InvalidDigit })]
```

- 使用完Partition后，结果还在Result存着，需要如下代码来取出来
```rust
fn main() {
    let strings = vec!["tofu", "93", "18"];
    let (numbers, errors): (Vec<_>, Vec<_>) = strings
        .into_iter()
        .map(|s| s.parse::<i32>())
        .partition(Result::is_ok);
    let numbers: Vec<_> = numbers.into_iter().map(Result::unwrap).collect();
    let errors: Vec<_> = errors.into_iter().map(Result::unwrap_err).collect();
    println!("Numbers: {:?}", numbers);
    println!("Errors: {:?}", errors);
}

```

## 函数如何返回一个堆内存

```rust
fn return_a_string() -> String {
    let s = String::from("hello"); 
    s // 返回s时，所有权已经被转移给返回值，所以不会被销毁
}
```

## deref trait
- 用于重载解引用操作符*
- 主要用于智能指针类型，让他们像普通引用一样工作

```rust
use std::ops::Deref;

struct MyBox<T>(T);

impl<T> MyBox<T> {
    fn new(x: T) -> MyBox<T> {
        MyBox(x)
    }
}

impl<T> Deref for MyBox<T> {
    type Target = T;
    
    fn deref(&self) -> &Self::Target {
        &self.0
    }
}

fn main() {
    let x = 5;
    let y = MyBox::new(x);
    
    // 使用 * 解引用
    assert_eq!(5, x);
    assert_eq!(5, *y);  // 实际调用 *(y.deref())
}
```

```rust
use std::ops::Deref;

struct Buffer {
    data: Vec<u8>,
    capacity: usize,
}

impl Buffer {
    fn new(capacity: usize) -> Self {
        Buffer {
            data: Vec::with_capacity(capacity),
            capacity,
        }
    }
    
    fn push(&mut self, value: u8) {
        if self.data.len() < self.capacity {
            self.data.push(value);
        }
    }
}

impl Deref for Buffer {
    type Target = [u8];
    
    fn deref(&self) -> &Self::Target {
        &self.data
    }
}

fn main() {
    let mut buf = Buffer::new(10);
    buf.push(1);
    buf.push(2);
    buf.push(3);
    
    // 可以直接使用 slice 的方法
    println!("Length: {}", buf.len());  // 调用 Vec::len
    println!("Is empty: {}", buf.is_empty());
    
    // 可以遍历
    for byte in buf.iter() {
        println!("{}", byte);
    }
}
```

## drop trait 
- 类似于c++析构函数
- 如果想提前释放资源, 使用std::mem::drop 
- std::mem::drop不会干扰rust自动清理机制，它通过接管所有权，并在调用后销毁它

## Rc<T>
- 只读访问，内部数据不可变
- 单线程

```rust
use std::rc::Rc;

// 创建 Rc
let a = Rc::new(42);
println!("引用计数: {}", Rc::strong_count(&a)); // 1

// 克隆（增加引用计数，不深拷贝数据）
let b = Rc::clone(&a);
println!("引用计数: {}", Rc::strong_count(&a)); // 2

{
    let c = Rc::clone(&a);
    println!("引用计数: {}", Rc::strong_count(&a)); // 3
} // c 离开作用域，计数减回 2

println!("最终值: {}", a); // 42
```

- `Rc::new(T)` - 创建新的引用计数指针
- `Rc::clone(&rc)` - 增加引用计数（浅拷贝）
- `Rc::strong_count(&rc)` - 获取引用计数（调试用）
- `Rc::try_unwrap(rc)` - 计数为1时取出内部值


## RefCell<T> 还需要再理解
- borrow()：获取不可变引用。如果当前存在可变引用，会导致运行时 panic。
- borrow_mut()：获取可变引用。如果当前存在其他引用（不可变或可变），也会导致运行时 panic。
- new(value)：创建一个新的 RefCell 包裹指定的值。

例子
```rust
use std::cell::RefCell;
fn main() {
   let data = RefCell::new(5);
   // 获取不可变引用
   let borrow1 = data.borrow();
   println!("Borrowed value: {}", borrow1);
   // 获取可变引用
   {
       let mut borrow_mut = data.borrow_mut();
       *borrow_mut += 1;
   }
   // 再次获取不可变引用
   let borrow2 = data.borrow();
   println!("Updated value: {}", borrow2);
}
```

### RefCell 的核心特性是 内部可变性，即允许在不可变引用的情况下修改数据。这在以下场景中非常有用：
- 当外部库的接口限制了使用不可变引用时，可以通过 RefCell 实现对内部数据的修改。
- 在复杂的程序中，数据可能需要被多个地方共享和修改，RefCell 提供了一种灵活的解决方案。
```rust
trait Messenger {
    fn send(&self, msg: &str);
}

struct MockMessenger {
    sent_messages: RefCell<Vec<String>>,
}

impl Messenger for MockMessenger {
    fn send(&self, msg: &str) {
        // &self 是不可变引用，但可以通过 RefCell 修改
        self.sent_messages.borrow_mut().push(msg.to_string());
    }
}
```

## 避免循环引用,使用weak ref
- weak 没有所有权，不影响引用计数和数据释放
- 不能直接读取数据，需要upgrade成Rc才行
- upgrade成功，返回Some(V), 失败返回None

```rust
    let strong = Rc::new(42);
    let weak = Rc::downgrade(&strong);
    
    // 升级弱引用
    if let Some(value) = weak.upgrade() {
        println!("值还存在: {}", value);
    }
    
    drop(strong); // 释放强引用
    
    // 现在升级会失败
    assert!(weak.upgrade().is_none());
```

## String
- 实现了Display这个trait的，都有to_string()方法来获得一个string
- `let s = String::from("hello");`
- 多个字符串拼接使用format宏 `let s = format!("{s1}-{s2}-{s3}");`
- rust不允许使用索引来访问String元素，因为字符是utf8编码，并且遍历是o(n)不是o(1)
- rust字符是unicode字符，字符存进字符串会进行utf8编码
```rust


```